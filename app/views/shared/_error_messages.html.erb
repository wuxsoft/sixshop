<% if target.errors.any? %>
 <div class="alert alert-danger alert-block">
   <h2>有 <%= target.errors.count %> 处问题导致无法提交:</h2>

   <ul>
   <% target.errors.full_messages.each do |msg| %>
     <li><%= msg %></li>
   <% end %>
   </ul>
 </div>
<% end %>